#
# ODE makefile (/.../watson.ibm.com/fs/users/G/germain/bluegene/sandboxes/my_stuff/src/BlueMatter/webapps/bluematter/com/ibm/bluematter/servlet/Makefile.ode)
# Generated by MakeMake (ODE3.0 9)
#
JAR_LIBRARIES = molservlet${JAR_SUFF}

JAR_OBJECTS =   \
  com/ibm/bluematter/servlet/MolSysServlet${CLASS_SUFF} \
  com/ibm/bluematter/servlet/MolSysQuery${CLASS_SUFF} \
  com/ibm/bluematter/servlet/MolSysView${CLASS_SUFF} \
  com/ibm/bluematter/servlet/MolUdfView${CLASS_SUFF} \
  com/ibm/bluematter/servlet/MolSiteView${CLASS_SUFF} \
  com/ibm/bluematter/servlet/ParamVocabView${CLASS_SUFF} \
  com/ibm/bluematter/servlet/CompileTimeQuery${CLASS_SUFF} \
  com/ibm/bluematter/servlet/CompileTimeTable${CLASS_SUFF} \

JAVA_PACKAGE_NAME = com.ibm.bluematter.servlet

# the Java class files to generate
JAVA_CLASSES += \
  MolSysServlet${CLASS_SUFF} \
  MolSysQuery${CLASS_SUFF} \
  MolSysView${CLASS_SUFF} \
  MolUdfView${CLASS_SUFF} \
  MolSiteView${CLASS_SUFF} \
  ParamVocabView${CLASS_SUFF} \
  CompileTimeQuery${CLASS_SUFF} \
  CompileTimeTable${CLASS_SUFF} \

IDIR = $(BG_WEB)/bluematter/WEB-INF/lib/

ILIST = $(JAR_LIBRARIES)

# the subdirs to traverse for the EXPINC pass
#EXPINC_SUBDIRS += 

# the subdirs to traverse for the OBJECTS pass
#OBJECTS_SUBDIRS += 

# the subdirs to traverse for the EXPLIB pass
#EXPLIB_SUBDIRS += 

# the subdirs to traverse for the STANDARD pass
#SUBDIRS += 


# include the ODE rules
.include <${RULES_MK}>

