
<Directory "/usr/local/src/subversion/dep_sources">
    Options Indexes FollowSymLinks
    AllowOverride None
    Order allow,deny
    Allow from all

AuthName "Authorized GSA ID Required"

AuthType Basic

AuthBasicProvider ldap

 AuthLDAPURL ldap://pokgsa.ibm.com:389/ou=gsa,o=ibm.com?uid?sub?(ObjectClass=*) NONE
require ldap-user rgermain germain2 bgf arayshu mariae tjcw pitman mnewton gdozsa mr198 lschneid zrlbmt ahomike armamida rgioios wagnerjo jimvo mmundy tinglett tgooding ataufer schickhj sameer77 morjan benk vsachde kjordan
</Directory>

<Location /svn>
          SetOutputFilter DEFLATE
          DAV svn
          SVNParentPath /svn

#
# Possible values for the Options directive are "None", "All",
# or any combination of:
#   Indexes Includes FollowSymLinks SymLinksifOwnerMatch ExecCGI MultiViews
#
# Note that "MultiViews" must be named *explicitly* --- "Options All"
# doesn't give it to you.
#
# The Options directive is both complicated and important.  Please see
# http://httpd.apache.org/docs-2.0/mod/core.html#options
# for more information.
#
    Options Indexes FollowSymLinks

#
# AllowOverride controls what directives may be placed in .htaccess files.
# It can be "All", "None", or any combination of the keywords:
#   Options FileInfo AuthConfig Limit
#
#    AllowOverride None

#
# Controls who can get stuff from this server.
#
    Order allow,deny
    Allow from all

AuthName "Authorized GSA ID Required"


AuthType Basic

AuthBasicProvider ldap

 AuthLDAPURL ldap://pokgsa.ibm.com:389/ou=gsa,o=ibm.com?uid?sub?(ObjectClass=*) NONE
require ldap-user rgermain germain2 bgf arayshu mariae tjcw pitman mnewton gdozsa mr198 lschneid zrlbmt ahomike armamida rgioios wagnerjo jimvo mmundy tinglett tgooding ataufer schickhj sameer77 morjan benk vsachde kjordan

</Location>

<Location /svn/test>
          SetOutputFilter DEFLATE

#
# Possible values for the Options directive are "None", "All",
# or any combination of:
#   Indexes Includes FollowSymLinks SymLinksifOwnerMatch ExecCGI MultiViews
#
# Note that "MultiViews" must be named *explicitly* --- "Options All"
# doesn't give it to you.
#
# The Options directive is both complicated and important.  Please see
# http://httpd.apache.org/docs-2.0/mod/core.html#options
# for more information.
#
    Options Indexes FollowSymLinks

#
# AllowOverride controls what directives may be placed in .htaccess files.
# It can be "All", "None", or any combination of the keywords:
#   Options FileInfo AuthConfig Limit
#
#    AllowOverride None

#
# Controls who can get stuff from this server.
#
    Order allow,deny
    Allow from all

AuthName "Authorized GSA ID Required"

AuthType Basic

AuthBasicProvider ldap

 AuthLDAPURL ldap://pokgsa.ibm.com:389/ou=gsa,o=ibm.com?uid?sub?(ObjectClass=*) NONE
require ldap-user rgermain

</Location>

<Location /svn/bluematter>
          SetOutputFilter DEFLATE

#
# Possible values for the Options directive are "None", "All",
# or any combination of:
#   Indexes Includes FollowSymLinks SymLinksifOwnerMatch ExecCGI MultiViews
#
# Note that "MultiViews" must be named *explicitly* --- "Options All"
# doesn't give it to you.
#
# The Options directive is both complicated and important.  Please see
# http://httpd.apache.org/docs-2.0/mod/core.html#options
# for more information.
#
    Options Indexes FollowSymLinks

#
# AllowOverride controls what directives may be placed in .htaccess files.
# It can be "All", "None", or any combination of the keywords:
#   Options FileInfo AuthConfig Limit
#
#    AllowOverride None

#
# Controls who can get stuff from this server.
#
    Order allow,deny
    Allow from all

AuthName "Authorized GSA ID Required"

AuthType Basic

AuthBasicProvider ldap

 AuthLDAPURL ldap://pokgsa.ibm.com:389/ou=gsa,o=ibm.com?uid?sub?(ObjectClass=*) NONE
require ldap-user rgermain bgf arayshu mariae tjcw pitera pitman mnewton gdozsa mr198 lschneid zrlbmt ahomike armamida rgioios wagnerjo jimvo mmundy tinglett tgooding ataufer schickhj sameer77 morjan benk vsachde kjordan

# AuthzSVNAccessFile /opt/apache2.2/conf/extra/svn_access.txt

</Location>


<Location /svn/customer>
          SetOutputFilter DEFLATE

#
# Possible values for the Options directive are "None", "All",
# or any combination of:
#   Indexes Includes FollowSymLinks SymLinksifOwnerMatch ExecCGI MultiViews
#
# Note that "MultiViews" must be named *explicitly* --- "Options All"
# doesn't give it to you.
#
# The Options directive is both complicated and important.  Please see
# http://httpd.apache.org/docs-2.0/mod/core.html#options
# for more information.
#
    Options Indexes FollowSymLinks

#
# AllowOverride controls what directives may be placed in .htaccess files.
# It can be "All", "None", or any combination of the keywords:
#   Options FileInfo AuthConfig Limit
#
#    AllowOverride None

#
# Controls who can get stuff from this server.
#
    Order allow,deny
    Allow from all

AuthName "Authorized GSA ID Required"

AuthType Basic

AuthBasicProvider ldap

 AuthLDAPURL ldap://pokgsa.ibm.com:389/ou=gsa,o=ibm.com?uid?sub?(ObjectClass=*) NONE
require ldap-user rgermain germain2

</Location>


<Location /svn/cardiac>
          SetOutputFilter DEFLATE

#
# Possible values for the Options directive are "None", "All",
# or any combination of:
#   Indexes Includes FollowSymLinks SymLinksifOwnerMatch ExecCGI MultiViews
#
# Note that "MultiViews" must be named *explicitly* --- "Options All"
# doesn't give it to you.
#
# The Options directive is both complicated and important.  Please see
# http://httpd.apache.org/docs-2.0/mod/core.html#options
# for more information.
#
    Options Indexes FollowSymLinks

#
# AllowOverride controls what directives may be placed in .htaccess files.
# It can be "All", "None", or any combination of the keywords:
#   Options FileInfo AuthConfig Limit
#
#    AllowOverride None

#
# Controls who can get stuff from this server.
#
    Order allow,deny
    Allow from all

AuthName "Authorized GSA ID Required"

AuthType Basic

AuthBasicProvider ldap

 AuthLDAPURL ldap://pokgsa.ibm.com:389/ou=gsa,o=ibm.com?uid?sub?(ObjectClass=*) NONE
require ldap-user rgermain bgf mr198 rjprill johnrice

</Location>
